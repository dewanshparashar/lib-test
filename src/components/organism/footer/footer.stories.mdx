import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from '@storybook/addon-docs';

import { Footer } from './footer';

# Footer

<Meta title="Cobalt/Footer" component={Footer} />

export const Template = () => <Footer />;

<Canvas>
  <Story
    name="Default"
    parameters={{
      backgrounds: {
        default: 'black',
      },
      docs: {
        page: null,
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Args

<Description of={Footer} />
<ArgsTable of={Footer} />
